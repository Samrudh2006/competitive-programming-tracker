import pandas as pd
import streamlit as st

uploaded_file = st.file_uploader("Upload a CSV file", type="csv")

if uploaded_file:
    problems = pd.read_csv(uploaded_file)
    st.dataframe(problems)
else:
    st.warning("Please upload the problems CSV file.")

